{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Transaction list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created at</th>
                <th>Amount</th>
                <th>Category</th>
                <th>Memo</th>
                <th>Payee</th>
                <th>Account</th>
                <th>Is flagged</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('transaction_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.amount }}</td>
                <td>{% if entity.category != null %}{{ entity.category.name }}{% else %}null{% endif %}</td>
                <td>{{ entity.memo }}</td>
                <td>{{ entity.payee }}</td>
                <td>{{ entity.account }}</td>
                <td>{{ entity.isFlagged }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('transaction_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('transaction_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('transaction_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
